toeminto moi() }
    pallaata "Moi";
{

toeminto onkoKaks(x) }
    pallaata (x justiisa 2);
{

toeminto piä() }
    lista ompi >> 1, >>2, 3<<, 3 <<;
    tulostappa(lista);


    lista ompi >> moi(), (moi() ynnättynä 2) <<;


    tulostappa(lista);

    lista ynnättynä 5;
    tulostappa(lista);

    lista ompi >> 1, 2, 2, 3 <<;
    tulostappa(laestappa(lista, onkoKaks));

    tulostappa(laestappa(lista, moi));

{
